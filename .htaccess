<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /App_Fichaje-bar/

    # Redirigir todo a api/index.php
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ api/index.php/$1 [QSA,L]
</IfModule>
